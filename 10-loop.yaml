---
- name: installing packages
  hosts: localhost
  become: true
  tasks:
  - name: installing packages 
    ansible.builtin.dnf:
      name: "{{item}}" 
      state: present
    loop:
    - mongodb
    - nginx
    - postfix
    - mysql
    - git
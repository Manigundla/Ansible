---
- name: installing and running nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.yum:
      name: nginx
      state: present
  - name: start nginx
    service:
      name: nginx
      state: started